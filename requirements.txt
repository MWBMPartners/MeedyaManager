# ============================================================================
# File: /requirements.txt
# (C) 2025-2026 MWBM Partners Ltd (d/b/a MW Services)
#
# Description:
# Core runtime and development dependencies for the MeedyaManager project.
# Requires Python 3.14+ (bundled via Nuitka for distribution).
# ============================================================================

# --- Core Runtime Dependencies ---
pymediainfo>=6.0          # Metadata extraction via MediaInfo library
mutagen>=1.47             # Tag reading/writing (ID3, MP4, FLAC, OGG)
watchdog>=4.0             # Real-time filesystem event monitoring
json5>=0.9                # JSON5 config file parsing (supports comments)
python-dotenv>=1.0.0      # .env file loading for API keys and overrides
colorama>=0.4.6           # Cross-platform terminal colour output
tqdm>=4.66                # Progress bars for batch scanning operations
rich>=13.0                # Enhanced CLI formatting, tables, and panels
click>=8.1                # CLI framework for commands and arguments
pydantic>=2.5             # Settings/config validation and type safety

# --- M5: HTTP & Retry ---
httpx>=0.28               # Modern async/sync HTTP client for provider APIs
tenacity>=9.0             # Retry logic with exponential backoff for API calls

# --- M5: Music Provider Libraries ---
spotipy>=2.24             # Spotify Web API (OAuth2 Client Credentials)
musicbrainzngs>=0.7       # MusicBrainz API (User-Agent based access)
deezer-python>=7.0        # Deezer API (public search, no auth for search)
tidalapi>=0.8             # Tidal API (OAuth2.1 session management)
ytmusicapi>=1.8           # YouTube Music (unofficial, browser-cookie auth)
shazamio>=0.7             # Shazam audio fingerprinting (async)

# --- M5: Video Provider Libraries ---
tmdbsimple>=2.9           # TheMovieDB API (API key auth)

# --- M5: Auth & Security ---
pyjwt[crypto]>=2.9        # JWT token generation (Apple Music ES256)
cryptography>=43.0        # Fernet encryption + ES256 key handling
keyring>=25.0             # OS-native credential storage (Keychain/Credential Manager)

# --- M5: Matching ---
fuzzywuzzy>=0.18          # Fuzzy string matching for result scoring
python-Levenshtein>=0.25  # C-accelerated Levenshtein distance calculation

# --- Testing Dependencies ---
pytest>=8.0               # Unit testing framework
pytest-cov>=5.0           # Code coverage reporting

# --- UI Dependencies ---
PySide6>=6.7              # Qt6 GUI framework (native platform styling)
darkdetect>=0.8           # System dark/light mode detection

# macOS-only (Liquid Glass support — install manually):
# pyobjc-framework-Cocoa>=10.0  # Native AppKit access for NSGlassEffectView
# pyobjc-framework-Quartz>=10.0 # Core Animation layer access

# --- M5: Optional Provider Libraries (GPL-2.0, not bundled by default) ---
# cinemagoer>=2023.5      # IMDb data access (GPL-2.0 — install separately)

# --- Build Dependencies (install separately for packaging) ---
# nuitka>=2.5             # Python-to-C compiler for native standalone builds
# ordered-set>=4.1        # Nuitka optimisation dependency
